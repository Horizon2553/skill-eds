/*
 * Project Detail Block
 * Delegates entirely to browse-projects.js — see that file for why
 * (EDS resolves a block's code by its own class name, so this file has
 * to exist for any page still authored as "Project Detail", even
 * though the real implementation lives in browse-projects.js).
 */
export { default } from '../browse-projects/browse-projects.js';
