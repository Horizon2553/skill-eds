/*
 * Hire Talent Block
 * Delegates entirely to browse-projects.js — same page shape (search +
 * filter + sort + card grid) as Browse Projects, so the real
 * implementation lives there. See that file for details.
 */
export { default } from '../browse-projects/browse-projects.js';
